# Enable the rewrite engine
RewriteEngine On

# Check if the request is for the sql folder
RewriteCond %{REQUEST_URI} ^/flowershop/sql/ [NC]

# If the request is for the sql folder, deny access
RewriteRule .* - [F]



